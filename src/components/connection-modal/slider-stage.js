const sliderStage = document.getElementById('slider-stage');
let rectangle = document.createElement('rect');
rectangle.setAttribute('fill', 'red');
console.log('I RUN');
sliderStage.appendChild(rectangle);